const letras =['a', 'b'];

//agregar al final del Array
letras.push('c', 'd');

//agregar al inicio del array
letras.unshift('y', 'z');

//agregar en el lugar segun un indice que determinemos
letras.splice(3, 0, 1,2)

console.log(letras);